## Reverse String Program

  public static void main(String [] args){
      
      String input ="Dahnraj";
      String rev = "";
      
      for(int i=input.length()-1;i>=0;i--){
        rev +=input.charAt(i);
      }
      
      System.out.println(rev);
    }


## Reverse each word individually without changing their positions in the sentence.

public class Main {
    public static void main(String[] args) {
        String input = "Dahnraj Kamble learning java program";
        String[] words = input.split(" ");
        String result = "";

        for (String word : words) {
            String reversedWord = "";
            for (int i = word.length() - 1; i >= 0; i--) {
                reversedWord += word.charAt(i);
            }
            result += reversedWord + " ";
        }

        // Trim trailing space
        System.out.println(result.trim());
    }
}
